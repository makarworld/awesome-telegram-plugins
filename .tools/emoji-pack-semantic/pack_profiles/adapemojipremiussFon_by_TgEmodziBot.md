# adapemojipremiussFon_by_TgEmodziBot

Пак **сохранён** в `output/adapemojipremiussFon_by_TgEmodziBot/` (120 emoji).

**В общий декор плагина не входит** — только выбранные горизонтальные связки для **мур/UwU-стиля** (`mur` в `CUSTOM_EMOJI`).

## Мур-полосы (CuteMessages)

| Индексы | combo / назначение |
|---------|-------------------|
| #009–#013 | `media_player` — панель плеера |
| #061–#064 | `blur_text` — размытая текстовая полоса |
| #074–#076 | хвост `bubbles` (без #073) |
| #093–#096 | `media_player_fon` — фоновая полоса |
| #105–#108 | spacer-лента (технический зазор) |

Условие: `use_premium_emoji` + `enable_uwu_speak`, ~30% шанс полосы сверху/снизу.

См. `plugin_premium_picks.json` → `adapemojipremiussFon_by_TgEmodziBot`.
