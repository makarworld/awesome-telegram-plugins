Проверенно с помощью Pluggy Bot (pluggy_robot.t.me)

📛 Отчёт • Mistral

✦ Метаданные плагина ✦
┌ Название: Plugin Verifier
├ ID: plugin_verifier
├ Версия: 2.4.8
├ Автор: @JasonVurhyz
├ Описание: Интеллектуальная локальная верификация и глубокий анализ плагинов

📅 Дата проверки: 2026-07-20 14:24

✦ Вердикт: 📛 Высокий риск
──────────────
☶ Назначение: Плагин выполняет локальный анализ и верификацию других плагинов для ExteraGram с использованием статического и динамического разбора кода.
──────────────
❏ Анализ:
• Плагин реализует функциональность анализа других плагинов, что требует доступа к их коду и бинарным данным. Основные риски связаны с сетевыми запросами к Supabase и загрузкой внешних ресурсов без должной проверки. Несмотря на отсутствие явного вредоносного кода, архитектура допускает потенциальные уязвимости для атак через подмену данных или MITM. Вердикт 'high' обусловлен комбинацией сетевых взаимодействий и обработки бинарных данных без достаточных мер безопасности.
• Плагин использует стандартные API ExteraGram для работы с UI, файлами и сетью, что соответствует безопасным практикам.
• Обнаружено подключение к внешнему серверу Supabase (SUPABASE_URL) с использованием публичного ключа (SUPABASE_ANON_KEY). Это потенциальный риск утечки метаданных анализа.
• Плагин выполняет глубокий анализ встроенных бинарных данных (DEX, ELF, ZIP), включая распаковку и парсинг байт-кода, что может быть использовано для скрытого исполнения кода.
• Реализована загрузка и анализ удалённых ресурсов (например, .dex/.so файлов) без проверки их подписей или хешей, что создаёт риск подмены.
• Отсутствует явная валидация SSL-сертификатов при сетевых запросах, что уязвимо для MITM-атак.
• Используются потенциально опасные функции (например, `ast.parse`, `eval`-подобные конструкции в анализе кода), но без явного исполнения пользовательского ввода.
• Плагин сохраняет и обрабатывает файлы в локальных директориях, но без доступа к системным или пользовательским данным за пределами разрешённых API.
[20.07.2026 17:24] Pluggy: 🌐 Сетевые обращения (статический анализ)
Найдены упоминания: https=7, http=4, requests=3, socket=2, urllib=1
URL (пример): https://piehyevxsxgbukhhblwr.supabase.co, https://t.me/exteraSettings

Найдены домены/IP: 2
- piehyevxsxgbukhhblwr.supabase.co; регистрация: 2020-01-11 (6 лет назад); VT: malicious=0, suspicious=0
- t.me; страна IP: NL (Нидерланды); регистрация: 2010-05-20 (16 лет назад); VT: malicious=0, suspicious=0